# Force compilation from source
--no-binary secp256k1
secp256k1==0.14.0

# Custom fork with tracing (pinned to the verified commit; bump when the
# fork moves — rawbit's backend tests are the acceptance gate)
git+https://github.com/rawBit-io/python-bitcointx.git@20dab4f766ded7961f2c5c36647cd7c276cf3f1f#egg=python-bitcointx
